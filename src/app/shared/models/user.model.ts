export class User {
    id?: string;
    firstname?: string; // ? is used to define optional parameter
    lastname?: string;
    phone?: string;
    email: string;
    password: string;
    role?: string;
    lastSignIn?: Date;
    createdAt?: Date;
 
}

